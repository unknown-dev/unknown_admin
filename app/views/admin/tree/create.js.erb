var content = "<%= escape_javascript( nested_nodes resource.root.subtree.arrange(order: default_order) )%>";
$("#<%= dom_id (resource.root) %>").html(content);
$("#<%= dom_id (resource.root) %>").setupFormActions();